from django.shortcuts import render
from django.conf import settings


def lgpd(request):

    # context = {
    #     'lgpd_nome': 'HPB',
    #     'lgpd_dominio': settings.URL,
    #     'lgpd_cnpj': '00.248.915/0001-10',
    #     'lgpd_email': 'ouvidoria@hpb.com.br',
    # }

    return render(request, 'lgpd/lgpd.html')
