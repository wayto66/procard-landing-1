from .config import (
    LGPD_NOME,
    LGPD_DOMINIO,
    LGPD_CNPJ,
    LGPD_EMAIL,
)


def lgpd(request):
    return {
        'lgpd_nome': LGPD_NOME,
        'lgpd_dominio': LGPD_DOMINIO,
        'lgpd_cnpj': LGPD_CNPJ,
        'lgpd_email': LGPD_EMAIL,
    }
